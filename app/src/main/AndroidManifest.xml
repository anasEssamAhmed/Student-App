<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.offersonlineworld.student">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Student">
        <activity android:name=".re_course_student"></activity>
        <activity android:name=".Rs_show_student" />
        <activity android:name=".AddGrade" />
        <activity android:name=".AddCourse2" />
        <activity android:name=".AddFaculty" />
        <activity android:name=".AddStudent" />
        <activity android:name=".EditStudent" />
        <activity android:name=".addcourse" />
        <activity android:name=".home" />
        <activity android:name=".Register_account" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>